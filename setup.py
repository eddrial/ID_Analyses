from setuptools import setup, find_packages

setup(
      name="id_analyses",
      author = "Ed Rial",
      version = "0.1",
      packages = ['id_analyses'],
      install_requires = ['numpy','h5py'],
      )
#